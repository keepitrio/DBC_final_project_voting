<div class = "container-fluid">
  <h1><%= @team.pitch.title %></h1>

  <i><%= @team.pitch.description %></i>
  <br>
  <h5> Team Members </h5>
  <ul>
    <% @team.students.each do |s| %>
      <li><%= s.name %></li>
    <% end %>
  </ul>

    <%= link_to "Teams", teams_path, method: :get, class: "btn btn-primary" %>
</div>

