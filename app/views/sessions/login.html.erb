<div class="container-fluid ml-l">
	<h2> Login </h2>

  <% if @errors && @errors.any? %>
    <ul>
      <% @errors.each do |error| %>
      <li><%= error %></li>
      <% end %>
    </ul>
  <% end %>

  <%= render 'form', user: @user %>

<div class = "btn-group">
  <%= link_to 'Register', new_user_path, :class => "btn btn-primary btn-lg"  %>
</div>
</div>

